import { FillZerosPipe } from './fill-zeros.pipe';

describe('FillZerosPipe', () => {
  it('create an instance', () => {
    const pipe = new FillZerosPipe();
    expect(pipe).toBeTruthy();
  });
});
